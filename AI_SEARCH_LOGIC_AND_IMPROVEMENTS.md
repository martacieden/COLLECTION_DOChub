# Логіка роботи AI Search та пропозиції покращень

## 📋 Поточна логіка AI Search

### 1. Компонент AISearchResultsBlock

**Розташування:** `src/App.tsx` (рядки 1282-1463)

**Що робить:**
- Отримує запит користувача (`query`) та знайдені документи (`documents`)
- Використовує `detectSearchIntent()` для визначення наміру користувача
- Обчислює метадані документів через `calculateDocumentMetadata()`
- Відображає summary з інформацією про знайдені документи
- Показує 3 CTA кнопки, але **тільки одна працює** (Add collection)

### 2. Функція detectSearchIntent()

**Розташування:** `src/App.tsx` (рядки 1184-1223)

**Логіка:**
- Аналізує запит користувача на ключові слова
- Визначає намір: `search`, `create`, `manage`, `analyze`
- Визначає тип документа: `invoice`, `contract`, `permit`, `waiver`, `lien`, `change order`
- Визначає, чи це інвойси (`isInvoice`)

**Ключові слова:**
- Створення: `create`, `створи`, `make`, `зроби`, `new`, `новий`, `add`, `додай`
- Аналіз: `analyze`, `аналіз`, `summary`, `підсумок`, `report`, `звіт`, `insights`, `аналітика`
- Управління: `organize`, `організуй`, `manage`, `керуй`, `group`, `групуй`
- Інвойси: `invoice`, `інвойс`, `invoices`, `інвойси`, `bill`, `рахунок`

### 3. Функція calculateDocumentMetadata()

**Розташування:** `src/App.tsx` (рядки 1226-1280)

**Що обчислює:**
- Кількість документів
- Діапазон дат (мінімальна та максимальна дата)
- Топ-3 організації/vendors
- Загальну суму (якщо є)
- Валюту
- Статуси (completed, pending, other)

### 4. Поточні CTA кнопки в AISearchResultsBlock

**Рядки 1418-1459:**

1. ✅ **"Add collection"** - ПРАЦЮЄ
   - Викликає `onCreateCollection` з автоматично згенерованими правилами
   - Створює колекцію на основі типу документів

2. ❌ **"View documents"** - НЕ ПРАЦЮЄ
   - Кнопка без `onClick` обробника
   - Має відкривати таблицю з усіма знайденими документами

3. ❌ **"See summary analytics"** - НЕ ПРАЦЮЄ
   - Кнопка без `onClick` обробника
   - Має показувати детальну аналітику по знайдених документах

### 5. Як використовується компонент

**Розташування:** `src/App.tsx` (рядки 1668-1672)

```typescript
<AISearchResultsBlock 
  query={block.data.query} 
  documents={block.data.documents} 
  onCreateCollection={onCreateCollection}
/>
```

**Проблема:** Передається тільки `onCreateCollection`, але не передаються обробники для інших CTA.

---

## 💡 Пропозиції для покращення

### Варіант 1: Мінімальний (швидкий фікс)

**Що зробити:**
1. Додати пропси `onViewDocuments` та `onViewAnalytics` до `AISearchResultsBlock`
2. Підключити обробники до кнопок
3. Реалізувати навігацію:
   - `onViewDocuments` → перехід до `viewMode: 'all-documents'` з фільтром по знайдених документах
   - `onViewAnalytics` → відкриття модального вікна з аналітикою або перехід до спеціального view

**Плюси:**
- Швидко реалізувати
- Мінімальні зміни в коді
- Всі CTA стануть клікабельними

**Мінуси:**
- Можливо, потрібно буде додати стан для фільтрації документів

---

### Варіант 2: Розширений (з контекстом)

**Що зробити:**
1. Додати всі необхідні пропси до `AISearchResultsBlock`:
   - `onViewDocuments?: () => void`
   - `onViewAnalytics?: () => void`
   - `onExport?: () => void`
   - `onApplyFilters?: () => void`
   - `onShare?: () => void`

2. Реалізувати логіку:
   - **View documents:**
     - Зберігати ID знайдених документів в контексті
     - Переходити до `viewMode: 'all-documents'`
     - Застосовувати фільтр, щоб показувати тільки знайдені документи
     - Можливо, додати badge "Filtered by AI search"

   - **See summary analytics:**
     - Відкривати модальне вікно з детальною аналітикою
     - Показувати графіки, статистику, тренди
     - Можливо, використати існуючий компонент або створити новий

3. Додати інші корисні CTA (якщо потрібно):
   - Export (PDF/CSV)
   - Apply filters (відкрити FilterBar з заповненими фільтрами)
   - Share collection

**Плюси:**
- Більш повноцінне рішення
- Можливість розширення в майбутньому
- Кращий UX

**Мінуси:**
- Потребує більше часу на реалізацію
- Можливо, потрібні нові компоненти

---

### Варіант 3: Інтерактивний (з можливістю взаємодії)

**Що зробити:**
1. Зробити метадані клікабельними:
   - Клік на дату → застосувати фільтр по даті
   - Клік на vendor → застосувати фільтр по vendor
   - Клік на суму → показати детальну інформацію про суми

2. Додати quick actions:
   - Hover на документ → показати preview
   - Клік на кількість документів → перейти до списку

3. Додати контекстне меню:
   - Права кнопка миші на блок → додаткові опції

**Плюси:**
- Найкращий UX
- Інтуїтивна взаємодія
- Більше можливостей для користувача

**Мінуси:**
- Найбільш складне в реалізації
- Потребує дизайн-рішень

---

## 🎯 Рекомендований підхід

**Рекомендую Варіант 2 (Розширений)** з елементами Варіанту 1:

1. **Перший крок (швидкий фікс):**
   - Додати `onViewDocuments` та `onViewAnalytics` пропси
   - Підключити обробники
   - Реалізувати базову навігацію

2. **Другий крок (покращення):**
   - Додати фільтрацію документів при переході до "View documents"
   - Створити компонент для аналітики або використати існуючий

3. **Третій крок (опціонально):**
   - Зробити метадані інтерактивними
   - Додати додаткові CTA за потреби

---

## 📝 Технічні деталі для реалізації

### Для "View documents":

```typescript
// В App.tsx додати стан для AI-фільтра
const [aiSearchFilter, setAiSearchFilter] = useState<Set<string> | null>(null);

// В AISearchResultsBlock передати обробник
const handleViewDocuments = () => {
  const documentIds = new Set(documents.map(doc => doc.id));
  setAiSearchFilter(documentIds);
  setViewMode('all-documents');
};

// В AllDocumentsTable фільтрувати документи
const filteredDocs = aiSearchFilter 
  ? documents.filter(doc => aiSearchFilter.has(doc.id))
  : documents;
```

### Для "See summary analytics":

```typescript
// Варіант 1: Модальне вікно
const [showAnalyticsModal, setShowAnalyticsModal] = useState(false);

// Варіант 2: Перехід до нового view
const handleViewAnalytics = () => {
  setViewMode('analytics');
  setAnalyticsDocuments(documents);
};
```

---

## ❓ Питання для уточнення

1. Як саме має працювати "View documents"?
   - Просто перехід до таблиці з усіма документами?
   - Або з фільтром, щоб показувати тільки знайдені?

2. Що має показувати "See summary analytics"?
   - Графіки?
   - Таблиці зі статистикою?
   - Текстову аналітику?

3. Чи потрібні додаткові CTA кнопки?
   - Export?
   - Apply filters?
   - Share?

4. Чи потрібно зберігати стан пошуку?
   - Щоб користувач міг повернутися до результатів?


