# План реалізації модального вікна редагування правил

## 📋 Загальна мета
Створити модальне вікно, яке відкривається після генерації правил AI, дозволяє користувачу переглядати, редагувати та зберігати правила перед їх застосуванням до колекції.

## 🎯 Основні вимоги

### 1. Тригери відкриття модального вікна
- ✅ Користувач натискає "Generate rules" в `CollectionDetailView.tsx`
- ✅ AI генерує правила на основі опису колекції
- ✅ Після генерації автоматично відкривається модальне вікно з правилами

### 2. Структура модального вікна

#### Заголовок
- Назва: "AI-Generated Rules" або "Review Generated Rules"
- Badge з кількістю активних правил (наприклад, "5 active")
- Інформація про кількість документів, що відповідають правилам (наприклад, "42 documents matched")
- Кнопка закриття (X)

#### Контент
- **Секція "CONDITIONAL RULES"**
  - Кнопка "+ Add rule" для додавання нових правил вручну
  - Список згенерованих правил у форматі:
    ```
    [Dropdown: Type] [Dropdown: Operator] [Input: Value] [Toggle: Enabled] [Delete Button]
    ```
  - Кожне правило має:
    - Type dropdown: document_type, tags, client, keywords, date_range, vendor
    - Operator dropdown: is, contains, equals, not
    - Value input: текстовий ввід для значення
    - Enabled toggle: увімкнено/вимкнено правило
    - Delete button: видалення правила

#### Футер
- Кнопка "Cancel" - закриває модальне вікно без збереження
- Кнопка "Apply Rules" - зберігає правила до колекції та закриває модальне вікно

## 🔧 Технічна реалізація

### Крок 1: Створити компонент `RulesEditorModal.tsx`

**Розташування:** `src/components/RulesEditorModal.tsx`

**Props:**
```typescript
interface RulesEditorModalProps {
  isOpen: boolean;
  onClose: () => void;
  onSave: (rules: CollectionRule[]) => void;
  initialRules: CollectionRule[];
  matchedDocumentsCount?: number;
}
```

**Функціональність:**
- Відображення списку правил
- Редагування кожного правила (type, operator, value, enabled)
- Додавання нових правил
- Видалення правил
- Підрахунок активних правил
- Валідація правил перед збереженням

### Крок 2: Оновити `CollectionDetailView.tsx`

**Зміни в `handleGenerateRules`:**
1. Генерувати правила на основі `aiDescription` (використати логіку з `NewCollectionModal.tsx`)
2. Після генерації:
   - Зберігати згенеровані правила в state
   - Підрахувати кількість документів, що відповідають правилам
   - Відкрити модальне вікно `RulesEditorModal`

**Нові state:**
```typescript
const [generatedRules, setGeneratedRules] = useState<CollectionRule[]>([]);
const [isRulesModalOpen, setIsRulesModalOpen] = useState(false);
const [matchedDocCount, setMatchedDocCount] = useState(0);
```

**Нова функція для збереження правил:**
```typescript
const handleSaveRules = (rules: CollectionRule[]) => {
  // Оновити колекцію з новими правилами
  // Оновити документи, що відповідають правилам
  // Закрити модальне вікно
  // Показати toast про успіх
};
```

### Крок 3: Структура компонента `RulesEditorModal`

**Основні секції:**

1. **Header Section**
   - Заголовок "AI-Generated Rules"
   - Badge з кількістю активних правил
   - Текст про кількість відповідних документів
   - Кнопка закриття

2. **Rules List Section**
   - Заголовок "CONDITIONAL RULES"
   - Кнопка "+ Add rule"
   - Список правил з можливістю редагування

3. **Rule Item Component**
   - Type selector (dropdown)
   - Operator selector (dropdown)
   - Value input
   - Enabled toggle (switch)
   - Delete button

4. **Footer Section**
   - Кнопка "Cancel"
   - Кнопка "Apply Rules"

### Крок 4: Логіка генерації правил

**Використати існуючу логіку з `NewCollectionModal.tsx`:**
- Аналіз опису колекції
- Визначення типів документів
- Визначення тегів, клієнтів, ключових слів
- Створення правил на основі виявлених паттернів

**Додати:**
- Підрахунок документів, що відповідають правилам
- Відображення цієї інформації в модальному вікні

### Крок 5: Інтеграція з колекцією

**Після збереження правил:**
1. Оновити `collection.rules` з новими правилами
2. Знайти документи, що відповідають правилам
3. Додати `collectionId` до відповідних документів
4. Оновити `collection.count`
5. Оновити `collection.documentIds`
6. Зберегти зміни в localStorage (якщо використовується)
7. Оновити UI

## 📐 UI/UX деталі

### Дизайн модального вікна
- Ширина: `max-w-[700px]`
- Висота: `max-h-[80vh]`
- Padding: стандартний (24px)
- Скрол для списку правил, якщо їх багато

### Стилізація правил
- Кожне правило в окремому рядку
- Іконки для типів правил (як в `NewCollectionModal.tsx`)
- Кольорові badge для типів правил
- Hover ефекти для інтерактивних елементів

### Валідація
- Перевірка, що хоча б одне правило увімкнено
- Перевірка, що всі обов'язкові поля заповнені
- Показ помилок при невалідних даних

## 🔄 Потік роботи

1. Користувач вводить опис колекції
2. Натискає "Generate rules"
3. Показується індикатор завантаження ("Analyzing...")
4. AI генерує правила (симуляція ~2 секунди)
5. Відкривається модальне вікно з правилами
6. Користувач може:
   - Редагувати правила
   - Додавати нові правила
   - Видаляти правила
   - Увімкнути/вимкнути правила
7. Натискає "Apply Rules"
8. Правила зберігаються до колекції
9. Документи автоматично додаються до колекції
10. Модальне вікно закривається
11. Показується toast про успіх

## 📝 Чекліст реалізації

- [ ] Створити компонент `RulesEditorModal.tsx`
- [ ] Додати інтерфейси та типи
- [ ] Реалізувати UI для відображення правил
- [ ] Додати функціональність редагування правил
- [ ] Додати функціональність додавання правил
- [ ] Додати функціональність видалення правил
- [ ] Додати toggle для enabled/disabled
- [ ] Реалізувати логіку генерації правил в `CollectionDetailView.tsx`
- [ ] Інтегрувати модальне вікно з `CollectionDetailView.tsx`
- [ ] Реалізувати збереження правил до колекції
- [ ] Додати підрахунок відповідних документів
- [ ] Додати валідацію
- [ ] Додати обробку помилок
- [ ] Додати toast повідомлення
- [ ] Протестувати весь flow

## 🎨 Приклад структури компонента

```tsx
<RulesEditorModal
  isOpen={isRulesModalOpen}
  onClose={() => setIsRulesModalOpen(false)}
  onSave={handleSaveRules}
  initialRules={generatedRules}
  matchedDocumentsCount={matchedDocCount}
/>
```

## 🔗 Залежності

- Використати існуючі типи `CollectionRule` з `App.tsx`
- Використати іконки з `lucide-react`
- Використати стилі з Tailwind CSS
- Використати `toast` з `sonner` для повідомлень



